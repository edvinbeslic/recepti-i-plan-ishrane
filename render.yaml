services:
  - type: web
    name: recepti-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install -g json-server
    startCommand: json-server --watch db.json --host 0.0.0.0 --port 3001
    envVars:
      - key: PORT
        value: 3001